services:
  - type: web
    name: ayurmed
    runtime: node
    buildCommand: npm ci && npm run build && ls -la dist/
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
